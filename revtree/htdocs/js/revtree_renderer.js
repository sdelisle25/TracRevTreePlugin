(function() {

  /* Svg renderer object */
  var SvgRenderer = function(renderer)
  {
    this.renderer = renderer;
  };

  /* Publish SvgRenderer */
  window.SvgRenderer = SvgRenderer;

  SvgRenderer.prototype.arrow = function(renderer)
  {
  };
})();
